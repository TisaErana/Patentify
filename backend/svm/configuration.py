# Configuration:
MIN_TRAINING_SIZE = N_COMPONENTS = 4 # the minimum number of patents required to train the model, n_components = min(n_components, y_features) y_features = targets
MIN_AUTO_SAVE_CYCLES = 10